<header class="relative">
    <nav class="flex justify-between items-center h-16 dark:bg-gray-900 bg-gray-100 relative shadow-sm font-mono" role="navigation">
        <a href="/" class="pl-8 text-xl font-bold">Pokédex</a>

        <div class="px-4 cursor-pointer md:hidden" onclick="document.getElementById('mobileMenu').classList.toggle('hidden')">
            <svg class="w-6 h-6" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24"
                stroke="currentColor">
                <path d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </div>

        <div class="hidden md:block md:w-1/2 xl:w-3/4">
            <%- include('../layouts/search.ejs') -%>
        </div>

        <div class="pr-8 md:block hidden">
            <% if (route && route === 'add') { %>
                <a href="/" class="p-4 hover:text-gray-500">Home</a>
            <% } else { %>
                <a href="/add" class="p-4 hover:text-gray-500">Add pokemon</a>
            <% } %>
        </div>
    </nav>

    <ul id="mobileMenu" class="w-full absolute z-50 hidden md:hidden mt-2 mx-1 rounded-lg dark:bg-gray-900 bg-gray-100 border border-gray-300 dark:border-gray-700">
        <li class="p-2">
            <%- include('../layouts/search.ejs') -%>
        </li>
        
        <li class="px-2 pb-2">
            <% if (route && route === 'add') { %>
                <a href="/" class="block rounded hover:bg-blue-500 hover:text-white p-2">Home</a>
            <% } else { %>
                <a href="/add" class="block rounded hover:bg-blue-500 hover:text-white p-2">Add pokemon</a>
            <% } %>
        </li>
    </ul>
</header>